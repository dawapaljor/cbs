---
const {src, id} = Astro.props;
---


  <img src={src} alt="test" id="pic1" class="float-right w-[500px] p-2 shadow-xl relative"/>


<script >
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  gsap.registerPlugin(ScrollTrigger);

gsap.from("#pic1",{
  y: -700,
  scale:1.5,
  duration:8,
  pinSpacing: false,
  scrollTrigger:{
    trigger: ".content",
    start: "50% 50%",
    end: "50% 20%",
    toggleActions: "restart none none none",
    scrub: true,
    pin: "#pic1",
    pinSpacing: false,
    markers:true
  }
})

  // boxes.forEach((box) => {
  //   const img = box.querySelector('#pic1');
  //   if (img) {
  //     var tl = gsap.timeline({
  //       scrollTrigger: {
  //         trigger: box,
  //         start: "40% 95%",
  //         end: "50% 50%",
  //         scrub: true,
  //         markers: true
  //       }
  //     });
  //     tl.to(img, {
  //       top: "-120%",
  //       left: "0%"
  //     });
  //   }
  // });
</script>
